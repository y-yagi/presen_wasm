# 所感

* ブラウザですぐRubyが動くのはやはり便利
* 個人的には、バグ報告に重宝しそう
  * JSだとCodePenで再現ケース書いて報告する、が多かったが、Rubyでもそれが出来る
* Dockerコンテナの代わりにWasmバイナリを使用する、というケースが増えておきており、そういうケースでRubyが使えるのはありがたい
* RailsアプリケーションのWasmバイナリ化は、ブラウザでの使用はまだなんとも言えないが、コンテナの代わりでどうだろう
